
//4 pin numbers of 4 LEDs, add them by using or(|)
#define LED1 (1<<18)
#define LED2 (1<<20)
#define LED3 (1<<21)
#define LED4 (1<<23)

//Set up the direction for LEDs on GPIO and initialize
void setupLEDs();

//Turn off all LEDs
void LEDoff();

//Debugging using leds
//For direct binary value
void LEDdebug(uint8_t num); //0b0000 - 0b1111
//For natual reading direction
void LEDdebugR(uint8_t num); //0x0 - 0xf
